import React from 'react'
import ReactDOM from 'react-dom/client'
import GraphVisualisation from './Components/Graph'

import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  // <React.StrictMode>
    <GraphVisualisation/>
  // </React.StrictMode>,
)
